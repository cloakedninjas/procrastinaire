module Ala3.State {
    export class Score extends Phaser.State {

        init(endCondition, timeRemaining, workAccuracy) {
            console.log(endCondition, timeRemaining, workAccuracy);
        }

        create() {

        }


    }
}
